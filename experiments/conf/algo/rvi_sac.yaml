defaults:
  - common
  
id: RVI_SAC
params:
  critic_reset_hidden_dim: 64
  fq_gain: 1.0
  fq_reset_gain: 0.01
  fq_update_tau: 0.01
  target_reset_prob: 0.001
  use_reset_scheme: ${env.has_termination}
